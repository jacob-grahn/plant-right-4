{
  "name": "pr2-level-converter",
  "version": "4.0.0",
  "description": "",
  "main": "handler.js",
  "dependencies": {
    "axios": "^0.18.0",
    "canvas": "^2.0.0-alpha.14",
    "query-string": "^6.1.0"
  },
  "devDependencies": {
    "serverless": "^1.32.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "docker-deploy": "docker build . -t amazon-canvas && docker run --rm -v $PWD/.serverless:/root/.serverless -v ~/.aws:/root/.aws -v $PWD/pr2:/root/pr2 amazon-canvas npm run deploy"
  },
  "author": "Jacob Grahn",
  "license": "ISC"
}
